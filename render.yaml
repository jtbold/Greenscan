services:
  - type: web
    name: greenscan
    runtime: python
    buildCommand: bash render_build.sh
    startCommand: gunicorn app:app
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
